--- /dev/null
+++ /dev/null
@@ -1,5 +1,7 @@
 class WalletConnectService {
   Future<void> initialize() async {
+    final stopwatch = Stopwatch()..start();
+    print('�� Starting WalletConnect initialization...');
     
     // Existing initialization code
     await walletConnect.connect();
@@ -8,6 +10,9 @@
     await _setupWeb3();
     await _checkNetwork();
     
+    stopwatch.stop();
+    print('✅ WalletConnect initialized in \${stopwatch.elapsedMilliseconds}ms');
+    
     _isInitialized = true;
   }
 }
