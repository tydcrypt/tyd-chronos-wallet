name: tyd_chronos_wallet
description: "A Dual-Wallet with Advanced Banking & Multi-Chain Cryptocurrency Platform"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.3.2
  web3dart: ^2.7.3
  web_socket_channel: ^3.0.0
  bip39: ^1.0.6
  bip32: ^2.0.0
  encrypt: ^5.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icon/
